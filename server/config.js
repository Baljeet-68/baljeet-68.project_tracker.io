// Toggle this to switch between local and live DB
const USE_LIVE_DB = true; // true = MariaDB, false = local

const USE_ENCRYPTION = true; // true = encrypt sensitive data, false = no encryption

module.exports = {
  USE_LIVE_DB,
  USE_ENCRYPTION
};